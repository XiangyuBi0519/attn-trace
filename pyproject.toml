# 只声明 build 后端，其余元数据（name/version/entry_points/deps）全部由 setup.py 提供。
# 这样两条安装路径都能走：
#   1) pip install -e .          （标准 dev 流程）
#   2) 复制到框架路径 + setup.py 安装 （公司内部部署管线）
[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"
